'use client';
import { useEffect, useRef, useState } from 'react';
import { ArrowUp } from 'lucide-react';
export default function ModernMotion(){const [progress,setProgress]=useState(0);const [visible,setVisible]=useState(false);const rootRef=useRef<HTMLDivElement>(null);useEffect(()=>{const root=rootRef.current?.closest('.modern') as HTMLElement|null;if(!root)return;const reduce=window.matchMedia('(prefers-reduced-motion: reduce)');if(reduce.matches)return;root.classList.add('motion-ready');const nodes=Array.from(root.querySelectorAll<HTMLElement>('.section-heading,.solution-tabs,.product-card,.modern-range-photo,.resources>div,.modern-news-grid article,.modern-about-photo,.modern-about-stats,.modern-contact,.modern-footer-panorama,.about-page-hero>*,.about-company-grid>*,.about-experience-list article,.about-capability-grid article,.about-quality>*,.about-client-grid>div'));nodes.forEach((el,i)=>{el.classList.add('motion-reveal');el.style.setProperty('--reveal-delay',`${el.matches('.product-card,.modern-news-grid article')?(i%3)*90:0}ms`)});const countFrames=new Set<number>();const originals=new Map<HTMLElement,string>();const countObserver=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;const el=entry.target as HTMLElement;const original=el.textContent||'';originals.set(el,original);const value=Number(original.replace(/[^0-9]/g,''));const suffix=original.includes('+')?'+':'';const start=performance.now();const tick=(now:number)=>{const t=Math.min(1,(now-start)/1300);el.textContent=Math.round(value*(1-Math.pow(1-t,3))).toLocaleString('en-US')+suffix;if(t<1){const f=requestAnimationFrame(tick);countFrames.add(f)}else el.textContent=original};countFrames.add(requestAnimationFrame(tick));countObserver.unobserve(el)}),{threshold:.5});root.querySelectorAll<HTMLElement>('.modern-about-stats .stat strong').forEach(el=>countObserver.observe(el));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-revealed');observer.unobserve(entry.target)}}),{threshold:.08});nodes.forEach(el=>observer.observe(el));let frame=0;const photo=root.querySelector<HTMLElement>('.editorial-hero-photo');const onScroll=()=>{if(frame)return;frame=requestAnimationFrame(()=>{frame=0;const height=document.documentElement.scrollHeight-window.innerHeight;setProgress(height>0?Math.min(100,window.scrollY/height*100):0);setVisible(window.scrollY>650);if(photo){const hero=photo.parentElement!;const rect=hero.getBoundingClientRect();if(rect.bottom>0)photo.style.transform=`translate3d(0,${Math.max(0,-rect.top)*.18}px,0) scale(1.08)`}})};window.addEventListener('scroll',onScroll,{passive:true});onScroll();return()=>{countObserver.disconnect();countFrames.forEach(cancelAnimationFrame);originals.forEach((text,el)=>{el.textContent=text});observer.disconnect();window.removeEventListener('scroll',onScroll);cancelAnimationFrame(frame);root.classList.remove('motion-ready');nodes.forEach(el=>el.classList.remove('motion-reveal','is-revealed'));if(photo)photo.style.transform=''}} ,[]);return <div ref={rootRef} className="modern-motion-ui"><div className="modern-reading-progress" aria-hidden="true" style={{transform:`scaleX(${progress/100})`}}/>{visible&&<button className="modern-back-top" aria-label="Back to top" onClick={()=>window.scrollTo({top:0,behavior:window.matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'})}><ArrowUp size={20}/><span>Back to top</span></button>}</div>}
